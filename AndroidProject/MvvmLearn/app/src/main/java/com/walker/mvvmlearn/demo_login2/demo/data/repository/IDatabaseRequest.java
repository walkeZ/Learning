package com.walker.mvvmlearn.demo_login2.demo.data.repository;


// 本地数据库请求

/**
 * 为了扩展，这样写(在仓库里面)
 * 本地获取标准接口（在仓库里面），也就是本地的数据读取（包括网络，服务器数据等等）
 * 先定义一个interface是，将接口标准化
 */
public interface IDatabaseRequest {
    // TODO: 2024/5/20 可扩展

//    void insertStudents(Student... studs);
//    void updateStudents(Student... studs);
//    void deleteStudents(Student... studs);
//    void deleteAllStudents(Student... studs);
//    void queryAllStudents(Student... studs);
}
